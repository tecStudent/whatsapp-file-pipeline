# Terraform

This directory is reserved for the Oracle Cloud Infrastructure resources.

The initial infrastructure scope will include the VCN, subnet, network rules,
Ampere A1 compute instance, Object Storage bucket, IAM policies, and Vault
configuration.

