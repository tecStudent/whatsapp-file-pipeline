# Docker

This directory will contain the production proxy and container configuration.

The initial development environment is defined in the root `docker-compose.yml`.
Caddy, Redis, PostgreSQL, and the Celery worker will be added in later tasks.

